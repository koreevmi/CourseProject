<UserControl x:Class="ConstructionMaterialsManager.Views.Pages.SuppliersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="5" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="AddSupplierBtn" Content="➕ Добавить" ToolTip="Добавить поставщика" Click="AddSupplierBtn_Click" Margin="0,0,10,0" Padding="10,5" Background="#4361EE" Foreground="White"/>
                <Button x:Name="EditSupplierBtn" Content="✏️ Редактировать" ToolTip="Редактировать поставщика" Click="EditSupplierBtn_Click" Margin="0,0,10,0" Padding="10,5" Background="#4361EE" Foreground="White"/>
                <Button x:Name="DeleteSupplierBtn" Content="🗑️ Удалить" ToolTip="Удалить поставщика" Click="DeleteSupplierBtn_Click" Padding="10,5" Background="#4361EE" Foreground="White"/>
            </StackPanel>
        </Border>

        <!-- Filters -->
        <Border Grid.Row="1" BorderBrush="#E0E0E0" BorderThickness="1" Padding="10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <Grid>
                    <TextBox x:Name="SupplierFilterTextBox" Width="250" TextChanged="SupplierFilterTextBox_TextChanged" Margin="0,0,10,0"/>
                    <TextBlock x:Name="SupplierFilterWatermark" Text="🔍 Поиск по названию..." Margin="5,0,0,0" Foreground="#888" VerticalAlignment="Center" IsHitTestVisible="False"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- DataGrid -->
        <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="1">
            <DataGrid x:Name="SuppliersDataGrid" AutoGenerateColumns="False" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Контактное лицо" Binding="{Binding ContactPerson}" Width="Auto"/>
                    <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="Auto"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="Auto"/>
                    <DataGridTextColumn Header="Адрес" Binding="{Binding Address}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
