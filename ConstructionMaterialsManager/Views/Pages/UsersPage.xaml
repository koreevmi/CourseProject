<UserControl x:Class="ConstructionMaterialsManager.Views.Pages.UsersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="5" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="AddBtn" Content="➕ Добавить" Click="AddBtn_Click" Margin="0,0,10,0" Padding="10,5" Background="#4361EE" Foreground="White"/>
                <Button x:Name="EditBtn" Content="✏️ Редактировать" Click="EditBtn_Click" Margin="0,0,10,0" Padding="10,5" Background="#4361EE" Foreground="White"/>
                <Button x:Name="DeleteBtn" Content="🗑️ Удалить" Click="DeleteBtn_Click" Padding="10,5" Background="#4361EE" Foreground="White"/>
            </StackPanel>
        </Border>

        <!-- DataGrid -->
        <Border Grid.Row="1" BorderBrush="#E0E0E0" BorderThickness="1">
            <DataGrid x:Name="DataGrid" AutoGenerateColumns="False" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="*"/>
                    <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                    <DataGridTextColumn Header="Роль" Binding="{Binding Role}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
