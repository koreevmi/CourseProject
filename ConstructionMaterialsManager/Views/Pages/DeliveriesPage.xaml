<UserControl x:Class="ConstructionMaterialsManager.Views.Pages.DeliveriesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="5" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="AddDeliveryBtn" Content="➕ Добавить" ToolTip="Добавить поставку" Click="AddDeliveryBtn_Click"
                        Margin="0,0,10,0" Padding="10,5" Background="#4361EE" Foreground="White"/>
                <Button x:Name="EditDeliveryBtn" Content="✏️ Редактировать" ToolTip="Редактировать поставку" Click="EditDeliveryBtn_Click"
                        Margin="0,0,10,0" Padding="10,5" Background="#4361EE" Foreground="White"/>
                <Button x:Name="DeleteDeliveryBtn" Content="🗑️ Удалить" ToolTip="Удалить поставку" Click="DeleteDeliveryBtn_Click"
                        Padding="10,5" Background="#4361EE" Foreground="White"/>
            </StackPanel>
        </Border>

        <!-- Filters -->
        <Border Grid.Row="1" BorderBrush="#E0E0E0" BorderThickness="1" Padding="10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <Grid>
                    <TextBox x:Name="DeliveryFilterTextBox" Width="200" TextChanged="DeliveryFilterTextBox_TextChanged" Margin="0,0,10,0"/>
                    <TextBlock x:Name="DeliveryFilterWatermark" Text="🔍 Поиск по материалу..." Margin="5,0,0,0"
                               Foreground="#888" VerticalAlignment="Center" IsHitTestVisible="False"/>
                </Grid>
                <ComboBox x:Name="SupplierFilter" Width="200" Margin="0,0,10,0" DisplayMemberPath="Name" SelectedValuePath="SupplierId"
                          SelectionChanged="SupplierFilter_SelectionChanged"/>
                <StackPanel Orientation="Horizontal">
                    <Label Content="📅 Дата с:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <DatePicker x:Name="StartDatePicker" Width="120" Margin="0,0,10,0" SelectedDateChanged="DatePicker_SelectedDateChanged"/>
                    <Label Content="по:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <DatePicker x:Name="EndDatePicker" Width="120" SelectedDateChanged="DatePicker_SelectedDateChanged"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- DataGrid -->
        <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="1">
            <DataGrid x:Name="DeliveriesDataGrid" AutoGenerateColumns="False" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Материал" Binding="{Binding Material.Name}" Width="*"/>
                    <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="Auto"/>
                    <DataGridTextColumn Header="Дата поставки" Binding="{Binding DeliveryDate, StringFormat=dd.MM.yyyy}" Width="Auto"/>
                    <DataGridTextColumn Header="Поставщик" Binding="{Binding Supplier.Name}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
